/* JSHint configurations */
/* jshint esversion: 6 */
/* jshint -W097 */

/**
 * Created by Leandro Luque on 26/01/2017.
 */

'use strict';

import GraphicalElement from "./graphical-element";
import {validateArgumentsObject} from "./util";

/*
export default class NewVerticalGroup extends GraphicalElement {

    constructor(argumentsObject) {
        if (!argumentsObject || argumentsObject === null) {
            argumentsObject = {};
        }
        validateArgumentsObject(argumentsObject, 'x', 0);

        super(argumentsObject.x, argumentsObject.y, 0, 0)
    }

}
*/
